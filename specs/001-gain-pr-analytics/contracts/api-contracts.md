# API Contracts

See `docs/github-api/api-field-catalog.yaml` for field-level provenance. Query definitions SHALL be versioned in source control. Each query SHALL document scope, arguments, selected fields, expected pagination connections, and expected error handling.
