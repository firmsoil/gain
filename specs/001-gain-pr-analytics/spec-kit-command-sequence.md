# Exact Spec Kit Command Sequence

```text
/speckit.constitution
/speckit.specify
/speckit.clarify
/speckit.plan
/speckit.checklist
/speckit.tasks
/speckit.analyze
/speckit.implement
/speckit.converge
```

For a production feature, the full path is recommended. Current Spec Kit documentation identifies this longer path and describes `analyze` as a post-task, read-only consistency gate and `converge` as the completeness loop. citeturn319563search7turn319563search8
